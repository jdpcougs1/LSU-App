namespace LSU_App
{
    // Main form class for the LSU_App application.
    // This form serves as the entry point for the application's user interface.
    public partial class Form1 : Form
    {
        // Constructor for Form1.
        // Initializes the form and its components.
        public Form1()
        {
            InitializeComponent(); // Sets up the UI components defined in the designer file.
        }

        // Event handler for the form's Load event.
        // This method is called when the form is first loaded into memory and displayed.
        // Place any initialization logic or startup code here.
        private void Form1_Load(object sender, EventArgs e)
        {

        }
    }
}
